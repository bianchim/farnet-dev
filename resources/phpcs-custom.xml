<?xml version="1.0"?>
<!-- See http://pear.php.net/manual/en/package.php.php-codesniffer.annotated-ruleset.php -->
<!-- =================================================================================== -->
<!-- This file may be used only with explicit permission of the Quality Assurance team!  -->
<!-- Each added exclude pattern needs to be referenced with a MULTISITE ticket where we  -->
<!-- can confirm the allowance and check the reasoning behind it.                        -->
<!--                                                                                     -->
<!-- Always avoid completely disabling rules and find a way to skip the affected files   -->
<!-- for the specified rule that is blocking your commit.                                -->
<!-- =================================================================================== -->
<ruleset name="Subsite">
    <description>Custom ruleset for subsite usage.</description>

    <!-- New guidelines allow for use of project property. -->
    <!-- Remove comment to disable the project check on info files.
    <rule ref="Drupal.InfoFiles.AutoAddedKeys.Project">
        <severity>0</severity>
    </rule>
    -->

    <!-- New guidelines allow for use of version property. -->
    <!-- Remove comment to disable the version check on info files.
    <rule ref="Drupal.InfoFiles.AutoAddedKeys.Version">
        <severity>0</severity>
    </rule>
    -->

    <!-- Farnet Exclude Files -->
    <exclude-pattern>*.features.menu_links.inc</exclude-pattern>
    <exclude-pattern>*.farnet_core.install</exclude-pattern>
    <exclude-pattern>*.farnet_faq.module</exclude-pattern>
    <exclude-pattern>*.features.fe_block_boxes.inc</exclude-pattern>
    <exclude-pattern>*.farnet_organisation_import.install</exclude-pattern>
    <!-- exclude-pattern>*.template.php</exclude-pattern -->
    <!-- Farnet theme excludes -->
    <exclude-pattern>farnet/framework/*</exclude-pattern>

    <exclude-pattern>lib/modules/nexteuropa_subscriptions/*</exclude-pattern>

</ruleset>