<?php
/**
 * @file
 * Install feature file.
 */

/**
 * Implements hook_install().
 */
function farnet_article_install() {
  // Remove old body field (now we use the new nexteuropa body field).
  if ($instance = field_info_instance('node', 'body', 'article')) {
    field_delete_instance($instance);
  }
}
